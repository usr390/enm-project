import { BlogPreviewPipe } from './blog-preview.pipe';

describe('BlogPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new BlogPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
