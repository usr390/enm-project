import { DateTimeToWordifiedMonthFullPipe } from './date-time-to-wordified-month-full.pipe';

describe('DateTimeToWordifiedMonthFullPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeToWordifiedMonthFullPipe();
    expect(pipe).toBeTruthy();
  });
});
