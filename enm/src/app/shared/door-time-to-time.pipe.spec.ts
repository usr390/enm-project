import { DoorTimeToTimePipe } from './door-time-to-time.pipe';

describe('DoorTimeToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DoorTimeToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
