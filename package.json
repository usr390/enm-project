{
  "scripts": {
    "clean-install": "cd enm && npm ci && cd ./../api-enm && pnpm install",
    "build": "cd enm && npm run build && cd ./../api-enm && pnpm run build",
    "test": "cd enm && npm run test"
  },
  "dependencies": {
    "primeflex": "^3.3.1"
  }
}
