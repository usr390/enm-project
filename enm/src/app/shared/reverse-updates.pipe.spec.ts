import { ReverseUpdatesPipe } from './reverse-updates.pipe';

describe('ReverseUpdatesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseUpdatesPipe();
    expect(pipe).toBeTruthy();
  });
});
