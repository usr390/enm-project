import { GenreListPipePipe } from './genre-list-pipe.pipe';

describe('GenreListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GenreListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
