import { ListedHowLongAgoPipe } from './listed-how-long-ago.pipe';

describe('ListedHowLongAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new ListedHowLongAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
